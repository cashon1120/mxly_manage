import {StyleSheet} from 'react-native';

const globalStyle = StyleSheet.create({
  flexBox: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
  flex_1: {
    flex: 1,
  },
});

export default globalStyle;
